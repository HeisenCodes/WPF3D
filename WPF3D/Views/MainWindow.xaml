<Window x:Class="WPF3D.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF3D"
        xmlns:helix="http://helix-toolkit.org/wpf"
        xmlns:h="http://helixToolkit.org/wpf"
        xmlns:v="clr-namespace:WPF3D.Views"
        xmlns:vm="clr-namespace:WPF3D.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}">
    <Grid>
        <helix:HelixViewport3D
            ShowFrameRate="True"
            ZoomExtentsWhenLoaded="True">

            <!-- Lighting -->
            <helix:SunLight/>

            <!-- Load OBJ model -->
            <helix:Teapot/>


        </helix:HelixViewport3D>
    </Grid>
</Window>
